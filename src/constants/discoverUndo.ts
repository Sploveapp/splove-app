/**
 * Undo / Retour gratuit en bêta (indépendant de `VITE_BETA_MODE`).
 * À false : paywall SPLove+, erreurs rewind explicites, pas de taps « à vide » forcés depuis la nav.
 */
export const IS_BETA_UNDO_FREE = true;
